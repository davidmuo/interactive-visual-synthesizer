
  # Interactive Visual Synthesizer App

  Interactive Visual Synthesizer App — a web-based audio/visual synthesizer built with React and Tone.js.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  